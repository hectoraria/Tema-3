package Ej03;

public class mayor_numero {
	
    	static int obtenerMaximo(int num1, int num2) {
        // Utilizamos el operador ternario para determinar el máximo
    		return  (num1 > num2) ? num1 : num2;
    }
}
